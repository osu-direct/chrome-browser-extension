{
  "name": "browser-extension",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist build dist-build chrome.zip firefox.zip",
    "build:ts": "tsc",
    "build:script": "tsc -p tsconfig.build.json",
    "build": "npm run clean && npm run build:ts && npm run build:script && node dist-build/build.js"
  },
  "devDependencies": {
    "@types/archiver": "^7.0.0",
    "@types/bun": "^1.3.9",
    "@types/chrome": "^0.1.36",
    "@types/fs-extra": "^11.0.4",
    "archiver": "^7.0.1",
    "fs-extra": "^11.3.3",
    "rimraf": "^6.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}